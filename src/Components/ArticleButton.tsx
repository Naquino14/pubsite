import React from 'react'
import '../App.css'

type Props = {
    aOnClick: () => void
}

const ArticleButton: React.FC<Props> = ({aOnClick}) => {
  // TODO: i have no idea what I wanted to do with this one ima be completely honest
  return (
    <div>ArButton</div>
  )
}

export default ArticleButton